{
  "name": "@greenhouse-mcp/scoped-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Per-actor read scoping wrapper for the Greenhouse MCP raw read client.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsx --test test/**/*.test.ts",
    "verify": "npm run typecheck && npm test"
  },
  "license": "MIT"
}
