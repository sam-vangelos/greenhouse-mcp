{
  "name": "greenhouse-mcp",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/control-plane",
    "packages/scoped-core",
    "packages/recruiter-mcp",
    "packages/action-mcp"
  ],
  "scripts": {
    "build": "npm run build -w @greenhouse-mcp/control-plane && npm run build -w @greenhouse-mcp/action-mcp",
    "typecheck": "npm run build && npm run typecheck --workspaces --if-present",
    "test": "npm run build && npm test --workspaces",
    "guard": "npm run guard --workspaces --if-present",
    "verify": "npm run build && npm run typecheck --workspaces --if-present && npm test --workspaces && npm run guard --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
